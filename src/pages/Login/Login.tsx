// src/pages/Login/Login.tsx
import React from 'react';
import AuthModal from '../../components/AuthModal/AuthModal';

const Login: React.FC = () => {
  return <AuthModal show={true} onClose={() => {}} />;
};

export default Login;
